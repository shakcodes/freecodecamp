//===================================pop() always removes the last element of an array. ========================
const car=["jaguar","thar","rangerover","mustang","fortuner"];
console.log(car.pop());
console.log(car);